---
name: 'Dummy'

playbook:
  - default

defaults:
  provision:
    provider_name: 'onprem'

cluster:
  name: 'Dummy'

hosts:
  - reserved_cpu: '100'
    im_mad: 'kvm'
    vm_mad: 'kvm'
    provision:
      hostname: ${provisions}-${provision_id}
